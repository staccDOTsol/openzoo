node_modules/
__pycache__/
*.log
grokui-app/dist/
lib/proxy.js.bak
.openzoo/
# machine-local live posteriors — shipped suite tables stay in
# lib/modelroute/outcomes.json and vendor/modelroute/outcomes.json
**/modelroute-outcomes.json

# secrets — never commit (telegram token, rpc keys)
.env
.env.local
.sonar/

# third-party app bundles — local Docker build context only, never in git
# (Dockerfile COPYs Grok Bot.app/Contents/Resources/app.asar from the working tree)
Grok Bot.app/
claude-app-patch/app/
claude-app-patch/*.asar*
.DS_Store
